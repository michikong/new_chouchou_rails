<h1>メッセージ一覧</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.id, contact_path(contact) %> : <%= contact.contact %></li>
  <% end %>
</ul>

<%= link_to '新規メッセージの投稿', new_contact_path %>